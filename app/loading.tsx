export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-black text-[var(--color-bone)]">
      <p className="font-mono text-[0.65rem] uppercase tracking-[0.45em]" role="status">
        Entering Ruined…
      </p>
    </main>
  );
}
