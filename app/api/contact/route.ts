import { NextResponse } from "next/server";
export async function POST(request:Request){const body=await request.json().catch(()=>null) as Record<string,unknown>|null;const name=typeof body?.name==="string"?body.name.trim().slice(0,100):"";const email=typeof body?.email==="string"?body.email.trim().slice(0,254):"";const message=typeof body?.message==="string"?body.message.trim().slice(0,4000):"";if(!name||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||message.length<20)return NextResponse.json({error:"Invalid submission"},{status:400});const endpoint=process.env.CONTACT_WEBHOOK_URL;if(!endpoint)return NextResponse.json({error:"Not configured"},{status:503});const response=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({to:"connect@theruinedproject.com",name,email,message,submittedAt:new Date().toISOString()})});return response.ok?NextResponse.json({ok:true}):NextResponse.json({error:"Delivery failed"},{status:502});}
