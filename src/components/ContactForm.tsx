"use client";
import { FormEvent, useState } from "react";
export default function ContactForm() { const [state,setState]=useState("idle"); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});setState(r.ok?"sent":"error");if(r.ok)e.currentTarget.reset();} return <form onSubmit={submit} className="grid gap-4 max-w-2xl"><input name="name" required placeholder="Name" className="border border-black/25 bg-transparent px-4 py-3 font-sans"/><input name="email" type="email" required placeholder="Email" className="border border-black/25 bg-transparent px-4 py-3 font-sans"/><textarea name="message" required minLength={20} rows={7} placeholder="Tell us what you would like to submit." className="border border-black/25 bg-transparent px-4 py-3 font-sans"/><button className="ui-heading w-fit border border-black px-5 py-3 text-xs">Send submission</button>{state==="sent"&&<p>Submission sent.</p>}{state==="error"&&<p>Form delivery is not connected yet.</p>}</form> }
